upstream mkdocs {
    server mkdocs:8000;
}

server {
    listen 80;
    server_name docs.1000texts.com;

    location / {
        proxy_pass http://mkdocs;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
