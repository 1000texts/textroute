# Shared webroot for Let's Encrypt HTTP-01 challenges. Served over plain HTTP
# and never behind auth, otherwise renewal fails.
location ^~ /.well-known/acme-challenge/ {
    root /var/www/certbot;
    default_type "text/plain";
}
